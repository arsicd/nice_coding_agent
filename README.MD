# Nice Coding Agent

Context building coding agent.

## Prerequisites

- [uv](https://docs.astral.sh/uv/) — Python package manager (replaces pip + venv)

Install uv if you don't have it:

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

## Setup
test_1